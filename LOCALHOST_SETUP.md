# Инструкция по запуску проекта локально

## Предварительные требования

- Node.js (v16+)
- MySQL сервер (локально)
- npm или yarn

## 1. Подготовка БД MySQL

### Вариант A: Использование существующей БД на PC

Если у вас уже установлена MySQL локально:

```bash
# Проверьте, что MySQL запущен
# Windows (PowerShell):
Get-Service MySQL*

# Linux/Mac:
sudo systemctl status mysql
```

### Вариант B: Создание новой БД

1. Откройте MySQL клиент:
```bash
mysql -u root -p
```

2. Создайте БД и пользователя:
```sql
CREATE DATABASE AppMessage;
CREATE USER 'admin'@'localhost' IDENTIFIED BY 'qweasdzxc';
GRANT ALL PRIVILEGES ON AppMessage.* TO 'admin'@'localhost';
FLUSH PRIVILEGES;
```

3. Импортируйте SQL схему (если есть файлы в backup_diplom):
```bash
mysql -u admin -p AppMessage < backup_diplom/messenger_tables_blyat.sql
mysql -u admin -p AppMessage < backup_diplom/AppMessage_tables_blyat.sql
```

## 2. Запуск бэкенда

```bash
# Переходим в папку бэкенда
cd backup_diplom/client/express

# Устанавливаем зависимости
npm install

# Запускаем сервер (порт 5001)
npm start
# или
node index.js
```

Результат: Бэкенд должен запуститься на `http://localhost:5001`

## 3. Запуск фронтенда

В другом терминале:

```bash
# Переходим в папку клиента
cd backup_diplom/client/client

# Устанавливаем зависимости
npm install

# Запускаем dev сервер (порт 3000)
npm run dev
```

Результат: Фронтенд должен открыться на `http://localhost:3000`

## 4. Проверка подключения

### Проверяем бэкенд:
```bash
curl http://localhost:5001/status
```

Ожидаемый ответ:
```json
{
  "status": "OK",
  "timestamp": "2025-12-25T...",
  "activeUsers": 0,
  "environment": "development"
}
```

### Проверяем фронтенд:
Откройте браузер: `http://localhost:3000`

## 5. Проверенные пути для подключения

✅ **Бэкенд использует localhost:**
- БД: `DB_HOST = localhost` (в `.env`)
- Порт: `PORT = 5001`

✅ **Фронтенд использует localhost:**
- API URL: `http://localhost:5001` (во всех компонентах)
- Статические файлы: `http://localhost:5001/uploads/...`

## 6. Возможные проблемы и решения

### Проблема: "ECONNREFUSED" - не подключается к БД
**Решение:** 
- Проверьте, что MySQL запущен: `mysql -u root -p -e "SELECT 1;"`
- Проверьте логин/пароль в `.env`
- Убедитесь, что пользователь `admin` имеет доступ

### Проблема: Ошибка 3006 при запуске
**Решение:**
- Удалите папку `node_modules` и `package-lock.json`
- Переустановите зависимости: `npm install`

### Проблема: Порт 5001 уже используется
**Решение:**
```bash
# Найти процесс на порту 5001 и убить его
# Windows:
netstat -ano | findstr :5001
taskkill /PID <PID> /F

# Linux/Mac:
lsof -i :5001
kill -9 <PID>
```

### Проблема: CORS ошибки
**Решение:** Убедитесь, что в `express/index.js` включена CORS поддержка (уже добавлено)

## 7. Структура папок

```
backup_diplom/
├── client/
│   ├── client/              # Фронтенд (React + Vite)
│   │   ├── src/
│   │   ├── package.json
│   │   └── .env             # ✅ Создан
│   │
│   └── express/             # Бэкенд (Node.js + Express)
│       ├── index.js
│       ├── db.js
│       ├── package.json
│       └── .env             # ✅ Обновлён (localhost)
│
└── uploads/                 # Папка для загруженных файлов
```

## 8. Сокращённый запуск (batch скрипт для Windows)

Создайте файл `start-dev.bat`:

```batch
@echo off
echo Starting backend...
start cmd /k "cd express && npm start"

timeout /t 2

echo Starting frontend...
start cmd /k "cd client && npm run dev"

echo Both servers starting...
```

Затем просто запустите `start-dev.bat`

## 9. Переменные окружения

### Backend (.env в express/):
```
PORT=5001
DB_HOST=localhost
DB_USER=admin
DB_PASSWORD=qweasdzxc
DB_NAME=AppMessage
DB_PORT=3306
DB_SSL=false
NODE_ENV=development
```

### Frontend (.env в client/):
```
VITE_API_URL=http://localhost:5001
VITE_SOCKET_URL=http://localhost:5001
```

## 10. Успешный запуск

Если всё работает, вы увидите:

**Бэкенд:**
```
Server running on http://localhost:5001
Connected to database
```

**Фронтенд:**
```
VITE v... ready in ... ms
➜  Local:   http://localhost:3000/
```

Откройте `http://localhost:3000` в браузере и начните использовать приложение!

---

**Проект готов к работе полностью локально на вашем ПК! 🎉**
